# Missing: llms.txt / llms-full.txt

`SKILL.md` references `reference/llms.txt` (quick guide) and `reference/llms-full.txt` (full API + TSL) as the official Three.js LLM documentation files. Those weren't included in the skill text you pasted, so they aren't fabricated here.

If you have these files (e.g. from wherever this skill package originally came from), drop them in this folder as `llms.txt` and `llms-full.txt`. Otherwise this skill still works fully without them — `core-patterns.md`, `input-patterns.md`, and `tsl-guide.md` cover the load-bearing patterns.
